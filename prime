#include <iostream>
using namespace std;
int main()
{
    //prime numbers
    int n,i;
    cout<<"Enter a number";
    cin>>n;
    for (i=2;i<n/2;i++)
    {
        if(n%i==0)
        {
            cout<<"Not prime";
            break;
        }
    }
    if (i==n)
    {cout<<"Prime";}
}
