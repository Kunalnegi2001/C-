// 1-3+5-7...
#include <iostream>

using namespace std;
int main()

{ 
   int n,i,s=0,sign=-1;
   cout<<"Enter the last number: ";
   cin>>n;
   for(i=1;i<=n;i=i+2)
   {sign= -1*sign;
   s=s+i*sign;
    }
       cout<<s;

return 0;
    
    
    
    
    
}
